# 运行 Agent 的主逻辑
# 检测输入
def run_agent(user_input):
    # 假设这是 Agent 的核心处理逻辑
    # 你可以根据需要修改逻辑
    result = f"Agent received: {user_input}"
    # 或者调用 CenterAgent_main.py 中的具体逻辑
    return result

# 传输用户输入的文本
def run_agent(user_input):

    result = f"Agent received: {user_input}"
    # 或者调用 CenterAgent_main.py 中的具体逻辑
    return result